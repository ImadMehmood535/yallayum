import ForgetPassword from "@/components/recover/ForgetPassword";
import React from "react";

export const metadata = {
  title: "Recover account",
  description: "Recover your account",
};

const Page = () => {
  return <ForgetPassword />;
};

export default Page;
